<?xml version="1.0" encoding="utf-8"?>

<ContentDialog
    x:Class="XrayUI.Views.AddRuleDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="using:XrayUI.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="添加规则"
    PrimaryButtonText="添加"
    CloseButtonText="取消"
    DefaultButton="Primary">

    <!-- A ContentDialog does not auto-scroll over-tall content, so wrap it.
         MaxHeight must stay below the CustomRulesWindow host height (620x460,
         see CustomRulesWindow.xaml.cs) minus the dialog chrome, or it clips again.
         Right padding gives the scrollbar its own gutter. -->
    <ScrollView MaxHeight="300"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Hidden"
                VerticalScrollMode="Auto"
                Padding="0,0,14,0">
    <!-- Fixed width so the dialog stays the same size across types; otherwise the
         process Browse row makes it wider than domain/IP. -->
    <StackPanel Spacing="16" Width="360">

        <StackPanel Spacing="6">
            <TextBlock loc:Localize.Text="AddRule_TypeLabelText.Text"
                       FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <ComboBox x:Name="TypeComboBox"
                      SelectedIndex="0"
                      HorizontalAlignment="Stretch">
                <ComboBoxItem loc:Localize.Content="AddRule_TypeDomainItem.Content" Tag="domain" />
                <ComboBoxItem Content="IP" Tag="ip" />
                <ComboBoxItem loc:Localize.Content="AddRule_TypeProcessItem.Content" Tag="process" />
            </ComboBox>
        </StackPanel>

        <StackPanel Spacing="6">
            <TextBlock loc:Localize.Text="AddRule_MatchLabelText.Text"
                       FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBox x:Name="MatchTextBox"
                     PlaceholderText="每行一个域名或规则，例如 youtube.com 或 geosite:cn"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     MinHeight="84"
                     MaxHeight="144"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" />

            <!-- Browse helpers (visible only when Type = process) -->
            <StackPanel x:Name="BrowsePanel"
                        Orientation="Horizontal"
                        Spacing="8"
                        Visibility="Collapsed">
                <Button x:Name="BrowseButton">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon x:Name="BrowseButtonIcon" Glyph="&#xE8E5;" FontSize="12" />
                        <TextBlock x:Name="BrowseButtonText" Text="浏览 exe..." />
                    </StackPanel>
                </Button>
                <ComboBox x:Name="BrowseFormatComboBox"
                          SelectedIndex="0"
                          MinWidth="130">
                    <ComboBoxItem loc:Localize.Content="AddRule_BrowseNameItem.Content" Tag="name" />
                    <ComboBoxItem loc:Localize.Content="AddRule_BrowsePathItem.Content" Tag="path" />
                    <ComboBoxItem loc:Localize.Content="AddRule_BrowseFolderItem.Content" Tag="folder" />
                </ComboBox>
            </StackPanel>

            <TextBlock x:Name="HintTextBlock"
                       Text="支持精确匹配及 regexp:、geosite: 前缀；每行填写一个值。"
                       FontSize="11"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       MaxLines="2"
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Spacing="6">
            <TextBlock loc:Localize.Text="AddRule_OutboundLabelText.Text"
                       FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <ComboBox x:Name="OutboundComboBox"
                      SelectedIndex="0"
                      HorizontalAlignment="Stretch">
                <ComboBoxItem loc:Localize.Content="AddRule_OutboundProxyItem.Content" Tag="proxy" />
                <ComboBoxItem loc:Localize.Content="AddRule_OutboundDirectItem.Content" Tag="direct" />
                <ComboBoxItem loc:Localize.Content="AddRule_OutboundBlockItem.Content" Tag="block" />
            </ComboBox>
        </StackPanel>

        <TextBlock x:Name="ErrorText"
                   loc:Localize.Text="AddRule_ErrorTextElem.Text"
                   Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                   FontSize="11"
                   Visibility="Collapsed" />

    </StackPanel>
    </ScrollView>
</ContentDialog>
